
/**
 * Media query breakpoints
 */
export const mediaBreaks = {
  desktop: 2080,
  laptop: 1680,
  tablet: 1024,
  mobile: 696,
  mobileS: 400,
};

/** 
 * Replaces spaces in a string with dashes 
 * */
export const updateKey = (str) => str.replace(/\s+/g, "-");
